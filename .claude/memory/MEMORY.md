# Repo Memory

> Conventions and facts specific to water-refilling-station.
> Injected every session via `.claude/hooks/load-memory.sh`.
> Per-fact `.md` files live in this folder; read a linked file when its fact is relevant.
